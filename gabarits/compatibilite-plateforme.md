# Note de compatibilite

## Noyau portable

- `name` et `description` seulement
- `SKILL.md` plus des fichiers de support optionnels

## Options specifiques aux plateformes

- frontmatter specifique a Claude :
- frontmatter optionnel OpenCode :
- emplacement generique `.agents/skills/` :

## Decision

- Rester par defaut sur le noyau portable sauf si une fonctionnalite specifique a une plateforme apporte une vraie valeur.

## Limites

- Indiquer clairement ce qui ne fonctionnera pas de la meme facon selon les outils.
